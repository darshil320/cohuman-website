export const HEADER_HEIGHT = 74;

/**
 * Height of the series PDP's sticky enquiry bar.
 *
 * Shared rather than hard-coded in the bar, because the assembly section pins a
 * full-height viewport underneath it: without reserving this much space the bar sits on
 * top of the 3D stage and cuts the bottom off the model.
 */
export const SERIES_BAR_HEIGHT = 68;
